#homebrew
mkdir /usr/local/homebrew && 
curl -L https://github.com/Homebrew/homebrew/tarball/master | 
tar xz --strip 1 -C /usr/local/homebrew
brew update
__________

#apache ant
brew install ant
__________

#git
brew install git bash-completion
__________

#mysql
brew install mysql
unset TMPDIR
mysql_install_db --verbose --user=`whoami` --basedir="$(brew --prefix mysql)" --datadir=/usr/local/var/mysql --tmpdir=/tmp
mysql.server start
mysql_secure_installation
  # allow user to input password
  # auto input 'y' to remove remote access, anon user, etc
__________

#vim
brew install vim
  # maybe snag a good PEP8 python ready .vimrc
  #+and throw it in the user home dir
__________

#python 2.7.9
brew install pyenv
pyenv install 2.7.9
pyenv global 2.7.9
__________


#copy following into ~/.gitconfig
[merge]
	keepBackup = false
	tool = custom
		
[mergetool "custom"]
	cmd = /Applications/p4merge.app/Contents/Resources/launchp4merge "$PWD/$BASE" "$PWD/$REMOTE" "$PWD/$LOCAL" "$PWD/$MERGED"
	keepTemporaries = false
	trustExitCode = false
	keepBackup = false
__________
#skype
__________

#java skd
  # we need to have the JDK available on a server for dl
  #+because automating dl from the oracle site is tricky
  #+and I think would be slower
__________

#lastpass
__________

#p4v visual client
__________

#xcode and xcode cmdline tools
__________


