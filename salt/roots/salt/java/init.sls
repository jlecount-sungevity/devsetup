# requires a source_url or jdk file - there is no default download location for
#+jdk

# maybe include the file in the repo (might be too big for a repo) or host it
#+on a server

{%- if java.source_url is defined %}

# Do Stuff


{%- endif %}
